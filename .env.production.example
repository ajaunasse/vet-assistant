# NeuroVet Production Environment Configuration
# Copiez ce fichier vers .env sur votre serveur de production
# ⚠️ NE COMMITEZ JAMAIS le fichier .env avec vos vraies clés !

# ==========================================
# OpenAI Configuration (OBLIGATOIRE)
# ==========================================
# Votre clé API OpenAI (commence par sk-)
OPENAI_API_KEY=sk-votre-clé-api-openai-ici

# ID de votre assistant configuré sur OpenAI Platform (commence par asst_)
OPENAI_ASSISTANT_ID=asst_votre_assistant_id_ici

# Modèle à utiliser (recommandé: gpt-4o)
OPENAI_MODEL=gpt-4o

# Température (0-2, plus bas = plus déterministe)
# Pour un contexte médical, 0.3 est recommandé
TEMPERATURE=0.3

# Nombre maximum de tokens dans la réponse
MAX_TOKENS=2000

# ==========================================
# Database Configuration (OBLIGATOIRE)
# ==========================================
# Format: mysql+aiomysql://username:password@host:port/database
# ⚠️ CHANGEZ LE MOT DE PASSE EN PRODUCTION !
DATABASE_URL=mysql+aiomysql://neurovet:VOTRE_MOT_DE_PASSE_SECURISE@db:3306/neurovet_db

# Afficher les requêtes SQL dans les logs (false en production)
SQL_ECHO=false

# ==========================================
# Frontend Configuration
# ==========================================
# URL de l'API backend (pour le frontend)
# En production avec domaine: https://neurovet.votredomaine.com
# En développement local: http://localhost:8000
REACT_APP_API_URL=https://neurovet.votredomaine.com

# ==========================================
# Docker Compose Configuration
# ==========================================
# Ces variables sont utilisées par docker-compose.yml
# ⚠️ CHANGEZ CES MOTS DE PASSE EN PRODUCTION !

# MySQL Root Password
MYSQL_ROOT_PASSWORD=VOTRE_MOT_DE_PASSE_ROOT_TRES_SECURISE

# MySQL Database
MYSQL_DATABASE=neurovet_db

# MySQL User
MYSQL_USER=neurovet

# MySQL Password (doit correspondre à celui dans DATABASE_URL)
MYSQL_PASSWORD=VOTRE_MOT_DE_PASSE_SECURISE

# ==========================================
# Application Settings (Optionnel)
# ==========================================

# Environment (development, staging, production)
ENVIRONMENT=production

# Debug mode (false en production)
DEBUG=false

# Log level (INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ==========================================
# Security Settings (Optionnel)
# ==========================================

# Secret key pour JWT ou sessions (générer avec: openssl rand -hex 32)
# SECRET_KEY=votre_clé_secrète_générée_aléatoirement

# CORS Origins (liste séparée par des virgules)
# En production, limitez aux domaines autorisés
# CORS_ORIGINS=https://neurovet.votredomaine.com,https://www.neurovet.votredomaine.com

# ==========================================
# Monitoring & Alerting (Optionnel)
# ==========================================

# Sentry DSN pour le tracking d'erreurs
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Email pour notifications (backups, erreurs, etc.)
# ADMIN_EMAIL=admin@votredomaine.com

# ==========================================
# Backup Configuration (Optionnel)
# ==========================================

# S3 ou autre stockage cloud pour backups
# AWS_ACCESS_KEY_ID=votre_access_key
# AWS_SECRET_ACCESS_KEY=votre_secret_key
# AWS_S3_BUCKET=neurovet-backups
# AWS_REGION=eu-west-1

# ==========================================
# Notes de Sécurité
# ==========================================
#
# 1. MOTS DE PASSE FORTS
#    - Minimum 16 caractères
#    - Mélange de majuscules, minuscules, chiffres, symboles
#    - Générateur: openssl rand -base64 32
#
# 2. PERMISSIONS FICHIERS
#    chmod 600 .env
#    chown neurovet:neurovet .env
#
# 3. NE JAMAIS COMMITER
#    - Vérifiez que .env est dans .gitignore
#    - Ne partagez jamais ce fichier
#
# 4. ROTATION DES CLÉS
#    - Changez les mots de passe régulièrement
#    - Gardez un backup chiffré des anciennes configurations
#
# 5. BACKUP
#    - Sauvegardez ce fichier de manière sécurisée
#    - Utilisez un gestionnaire de mots de passe ou vault
#
# ==========================================
# Commandes Utiles
# ==========================================
#
# Générer un mot de passe sécurisé:
# openssl rand -base64 32
#
# Générer une clé secrète:
# openssl rand -hex 32
#
# Vérifier les variables chargées:
# docker-compose config
#
# Tester la connexion à la base de données:
# docker-compose exec backend python -c "from src.infrastructure.database import database; import asyncio; asyncio.run(database.engine.connect())"
